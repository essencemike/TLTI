import { shallowMount } from '@vue/test-utils';
// import EuiBreadcrumb from '@/components/EuiBreadcrumb/index.vue';

describe('EuiBreadcrumb.vue', () => {
  it('renders props.msg when passed', () => {
    // const levelList: any[] = [];
    // const wrapper = shallowMount(EuiBreadcrumb, {
    //   data: () => ({ levelList }),
    // });
    // console.log(wrapper);
    // // expect(wrapper.text()).toMatch(msg);
  });
});
